slug: glazed-replace
name: Glazed replace flags
description: |
  These are the flags used to replace fields in the structured data processed.
flags:
  - name: replace-file
    type: string
    help: File with replacements
    default: ""
  - name: add-fields
    type: keyValue
    help: Add a constant field to the output
    default: {}
