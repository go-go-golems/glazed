slug: glazed-sort
name: Glazed sort flags
description: |
  These are the flags used to sort the structured data processed.
flags:
  - name: sort-by
    type: stringList
    help: Sort by a field (default ASC, use -sort-by=-field for DESC)
    default: []
