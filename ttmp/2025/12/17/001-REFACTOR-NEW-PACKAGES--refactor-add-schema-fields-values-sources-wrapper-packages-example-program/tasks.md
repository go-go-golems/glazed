# Tasks

## TODO

- [ ] Implement `glazed/pkg/cmds/schema` (type aliases + wrapper constructors)
- [ ] Implement `glazed/pkg/cmds/fields` (type aliases + field constructors/options + type constants)
- [ ] Implement `glazed/pkg/cmds/values` (type aliases + decoding/helpers around `InitializeStruct`)
- [ ] Implement `glazed/pkg/cmds/sources` (wrapper functions around `cmds/middlewares` + execution helpers)
- [ ] Add example program: `glazed/cmd/examples/refactor-new-packages` (env + cobra + struct decoding)
- [ ] Add compile/test coverage for the new wrapper packages and the example program
- [ ] (Follow-up) Update tutorials/docs to prefer the new packages (keep old docs valid)

